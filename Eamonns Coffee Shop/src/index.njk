---
layout: layouts/base.njk
title: Home
description: Eamonns Coffee Shop — sip stellar brews and read spacey stories.
pagination:
  data: collections.posts
  size: 1
  alias: post
permalink: /{% if pagination.pageNumber > 0 %}page/{{ pagination.pageNumber + 1 }}/{% endif %}
---
<section class="hero">
  <h1>Welcome to Eamonns Cofee Shop</h1>
  <p>Small sips. Big universe. Hand-crafted posts brewed fresh.</p>
  <p><a class="button" href="/blog/">Explore the Blog</a></p>
  <figure class="emoji">☕✨</figure>
  <hr />
  <h2>Latest Post</h2>
  {% if post %}
    <article class="featured-post">
      <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
      <p class="post-meta">
        <time datetime="{{ post.date | date('yyyy-MM-dd') }}">
          {{ post.date | date("MMMM d, yyyy") }}
        </time>
      </p>
      {% if post.data.description %}
        <p class="post-excerpt">{{ post.data.description }}</p>
      {% endif %}
      <p><a href="{{ post.url }}" class="read-more">Read more →</a></p>
    </article>
  {% endif %}

  {% if pagination.pages.length > 1 %}
    <nav class="pagination">
      {% if pagination.href.previous %}
        <a href="{{ pagination.href.previous }}" class="prev">← Previous Post</a>
      {% endif %}
      <span class="page-info">Post {{ pagination.pageNumber + 1 }} of {{ pagination.pages.length }}</span>
      {% if pagination.href.next %}
        <a href="{{ pagination.href.next }}" class="next">Next Post →</a>
      {% endif %}
    </nav>
  {% endif %}
</section>



